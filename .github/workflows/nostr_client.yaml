# ABOUTME: CI workflow for the nostr_client package.
# ABOUTME: Runs tests, formatting, and analysis only when nostr_client files change.

name: Nostr Client CI

on:
  push:
    branches: [main]
    paths:
      - "mobile/packages/nostr_client/**"
      - ".github/workflows/nostr_client.yaml"
  pull_request:
    branches: [main]
    paths:
      - "mobile/packages/nostr_client/**"
      - ".github/workflows/nostr_client.yaml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Nostr Client CI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile/packages/nostr_client/

    steps:
      - uses: actions/checkout@v4

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.38.3"
          channel: "stable"
          cache: true

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: ✨ Check formatting
        run: dart format --output=none --set-exit-if-changed lib test

      - name: 🕵️ Analyze code
        run: flutter analyze lib test

      - name: 🧪 Run tests
        run: flutter test
