name: upload_repository
description: Persistent video upload orchestration, retry, and recovery.
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  blossom_upload_service:
  dio: ^5.8.0
  hive_ce: ^2.6.0
  meta: ^1.18.0
  models:
  nostr_sdk:
  unified_logger:

dev_dependencies:
  build_runner: ^2.15.0
  fake_async: ^1.3.3
  flutter_test:
    sdk: flutter
  hive_ce_generator: ^1.6.0
  mocktail: ^1.0.3
  very_good_analysis: ^10.2.0
